# Скопируйте в deploy/.env.postgres на сервере (не коммитить).
# Используется: docker compose -f deploy/docker-compose.postgres.yml --env-file deploy/.env.postgres

POSTGRES_PASSWORD=ЗАМЕНИТЕ_НА_СИЛЬНЫЙ_ПАРОЛЬ
