# Task 03: API for web — summary

**Iteration:** iter-4-web-client

## What was done

- **GET `/conversations`** — list conversations for the web client.
- **GET `/knowledge-snapshots`** — list knowledge snapshots.
- **`guardian_links` router** with:
  - **GET `/guardian-links/children`** — children linked to the guardian.
  - **GET `/students/{id}/activity`** — activity for a student.
  - **GET `/students/{id}/progress`** — progress for a student.

## Status

Done.
