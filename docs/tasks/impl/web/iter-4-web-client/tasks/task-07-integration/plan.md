# Task 07: Integration, testing, docs — plan

**Iteration:** iter-4-web-client

## Goal

Close the iteration with quality gates and repository hygiene: static checks, documentation updates, and environment examples so others can run web + API consistently.

## Scope

- **TypeScript:** `tsc` / project build — no type errors on the web package.
- **Python:** **Ruff** lint clean on touched/backend scope as agreed.
- **Docs:** README (web section), **vision.md** (web stack, Make commands).
- **Repo:** `.gitignore` updates for web artifacts; **`.env.example`** including **`JWT_SECRET`** (and related vars).

## Out of scope

New features; large refactors outside integration/docs.
