# Backend Tasklist

## Обзор

**Backend** — ядро системы: доменная логика, сессии и прогресс, вызовы LLM, доступ к данным. Через REST API к нему подключаются **все клиенты** (сейчас Telegram-бот, затем веб) и концентрируются **интеграции** с внешними системами (OpenRouter и др. по [integrations.md](../integrations.md)). Продуктовый контекст — [idea.md](../idea.md).

Этот tasklist ведёт **итерации 2, 3 и 5** дорожной карты ([plan.md](../plan.md)) в виде последовательных волн внутри области backend. **Текущий фокус — итерация 2:** контракты и реализация API под два базовых сценария MVP (**вопрос к репетитору** и **фиксация выполненного домашнего задания**), состояние в памяти, затем рефакторинг бота на клиент API.

Детальные `plan.md` / `summary.md` по задачам — в `docs/tasks/impl/backend/<итерация>/tasks/task-NN-<slug>/` ([workflow.md](../templates/workflow.md)). Структура списка согласована с шаблоном [tasklist.md](../templates/tasklist.md) (в репозитории нет отдельного `tasklist-template.md`).

## Skills и `/find-skills`

На этапах **выбора стека** и **проектирования API** рекомендуется подбирать подходящие Cursor Skills через команду **`/find-skills`** (и при необходимости подключать их в сессии). Примеры тем: REST/OpenAPI и соглашения об ошибках, FastAPI и async, pytest и интеграционные тесты API — по смыслу задачи, без обязательного фиксированного набора.

## Легенда статусов

- 📋 **Planned** — запланирован
- 🚧 **In Progress** — в работе
- ✅ **Done** — завершён

## Связь с plan.md

| Этап (итерация) | Роль этого tasklist |
|-----------------|---------------------|
| **2 — MVP-учебные сценарии в боте** | Ведущая область: контракты и API под ключевые сценарии MVP, логика в памяти, рефакторинг бота под HTTP. Смежно: [tasklist-bot.md](tasklist-bot.md). |
| **3 — Персистентность и модель данных** | Слой БД, миграции, соответствие [data-model.md](../data-model.md) и [ADR-001](../adr/adr-001-database.md). |
| **5 — Расширение платформы** | Backend-инкременты (преподаватель, материалы, потоки) по приоритету из [idea.md](../idea.md); при необходимости — [tasklist-web.md](tasklist-web.md). |
| **1, 4, 6** | Не ведётся этим списком; сохраняйте стабильность контракта API для ит.4 (веб). |

## Makefile

**Правило:** появились новые команды локального запуска, тестов или обслуживания backend — добавь или обнови цель в **[Makefile](../../Makefile)** и отрази её в [README.md](../../README.md) и при необходимости в [vision.md](../vision.md) (раздел про команды). Не оставляй «висящие» сценарии только в чате или в одном файле.

---

## Итерация 2 (текущая волна): сводная таблица задач

| № | Задача | Кратко | Статус | Документы задачи |
|---|--------|--------|--------|------------------|
| 01 | Стек и соглашения | Выбор backend-стека, ADR при необходимости; обновление [.cursor/rules/conventions.mdc](../../.cursor/rules/conventions.mdc); согласование с [vision.md](../vision.md). | 📋 Planned | план \| summary — по созданию папки в `impl/backend/iter-2-mvp/...` |
| 02 | Контракты API | Два сценария: вопрос репетитору; фиксация выполненного ДЗ. OpenAPI, ошибки, версия API; опора на [data-model.md](../data-model.md) (`Conversation`, `Message`, `KnowledgeSnapshot` и др.). | 📋 Planned | план \| summary |
| 03 | Каркас backend | Каталог `backend/` по vision: `api/`, сервисы, `tutor/`, `llm/`; конфиг; health/readiness; точка входа сервиса. | ✅ Done | [план](impl/backend/iter-2-mvp/tasks/task-03-backend-scaffold/plan.md) \| [summary](impl/backend/iter-2-mvp/tasks/task-03-backend-scaffold/summary.md) |
| 04 | Базовые API-тесты | Тесты под сценарии сообщений, уже покрытые ботом (паритет, happy-path, ключевые ошибки валидации). | ✅ Done | [план](impl/backend/iter-2-mvp/tasks/task-04-api-tests/plan.md) \| [summary](impl/backend/iter-2-mvp/tasks/task-04-api-tests/summary.md) |
| 05 | Реализация API и логики | Эндпоинты, серверная логика, LLM stateless в ядре, OpenRouter из конфига ([integrations.md](../integrations.md)); история в tutor/сессии, не в LLM-клиенте. | ✅ Done | [план](impl/backend/iter-2-mvp/tasks/task-05-api-implementation/plan.md) \| [summary](impl/backend/iter-2-mvp/tasks/task-05-api-implementation/summary.md) |
| 06 | Документирование backend | Запуск, переменные окружения, OpenAPI, команды; актуализация перечня документов (см. матрицу ниже). | ✅ Done | [план](impl/backend/iter-2-mvp/tasks/task-06-backend-docs/plan.md) \| [summary](impl/backend/iter-2-mvp/tasks/task-06-backend-docs/summary.md) |
| 07 | Рефакторинг бота | HTTP-клиент к backend; идентификация пользователя (например `telegram_user_id`); бот — тонкий клиент ([vision.md](../vision.md)). | ✅ Done | [план](impl/backend/iter-2-mvp/tasks/task-07-bot-refactor-api/plan.md) \| [summary](impl/backend/iter-2-mvp/tasks/task-07-bot-refactor-api/summary.md) |
| 08 | Качество и практики | `ruff`, политика тестов и логирования; Makefile и документация синхронизированы с новыми командами. | ✅ Done | [план](impl/backend/iter-2-mvp/tasks/task-08-quality/plan.md) \| [summary](impl/backend/iter-2-mvp/tasks/task-08-quality/summary.md) |

---

## Актуализация проектной документации (по этапам)

Где уместно в ходе работ обновляй согласованность с реализацией (без противоречий [vision.md](../vision.md)):

| Файл | 01 Стек | 02 API | 03 Каркас | 04 Тесты | 05 Логика | 06 Доки | 07 Бот | 08 Качество |
|------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| [docs/vision.md](../vision.md) | ● | ○ | ● | ○ | ○ | ● | ● | ● |
| [docs/data-model.md](../data-model.md) | ○ | ● | ○ | ○ | ○ | ● | ○ | ○ |
| [docs/integrations.md](../integrations.md) | ○ | ○ | ○ | ○ | ● | ● | ○ | ○ |
| [docs/plan.md](../plan.md) | ○ | ○ | ○ | ○ | ○ | ● | ○ | ○ |
| [README.md](../../README.md) | ○ | ○ | ○ | ○ | ○ | ● | ● | ● |
| [.env.example](../../.env.example) | ● | ○ | ● | ○ | ● | ● | ○ | ○ |

● — обычно требуется правка · ○ — по необходимости.

---

## Задача 01: Стек и соглашения 📋

### Цель

Зафиксировать стек backend (Python, REST-фреймворк, зависимости, uv/make) и отразить его в проектных правилах так, чтобы дальнейшая реализация не расходилась с [vision.md](../vision.md).

### Состав работ

- [ ] Выбрать и зафиксировать веб-фреймворк и базовые библиотеки (в т.ч. генерация/сервирование OpenAPI, если применимо).
- [ ] При необходимости — запись в `docs/adr/` (отдельно от ADR по СУБД).
- [ ] Обновить [.cursor/rules/conventions.mdc](../../.cursor/rules/conventions.mdc): слои, имена модулей backend, команды, отсылки к stateless LLM и MVP без БД.
- [ ] Согласовать с [vision.md](../vision.md) формулировки по стеку и структуре `backend/`.

### Артефакты

- Зависимости в репозитории; при необходимости ADR; обновлённый `conventions.mdc`.

### Definition of Done (агент)

- Стек и ключевые библиотеки названы и не противоречат vision.
- `conventions.mdc` отражает фактический стек и пути; секреты по-прежнему только из `.env`.
- При появлении ADR — ссылка из vision или из adr/README по принятой практике.

### Definition of Done (пользователь)

- Можно открыть `conventions.mdc` и `vision.md` и увидеть согласованное описание backend-стека.
- При ADR — прочитать файл в `docs/adr/` и понять решение.

### Проверка после задачи

| Кто | Что |
|-----|-----|
| **Агент** | Прогон релевантных линтеров/установки зависимостей не ломается; ссылки в conventions ведут на существующие пути. |
| **Пользователь** | Просмотр `conventions.mdc`, при необходимости ADR. |
| **Команды** | `make install` или эквивалент из vision/README после появления backend-зависимостей. |
| **Результат** | Репозиторий и правила редактора описывают один и тот же стек. |

### Документы задачи

- 📋 План и 📝 Summary — `docs/tasks/impl/backend/iter-2-mvp/tasks/task-01-backend-stack/` (после создания)

---

## Задача 02: Контракты API (два базовых сценария) 📋

### Цель

Иметь согласованное описание публичного API до полной реализации: **(A) вопрос к репетитору** (диалог, сообщения, контекст сессии) и **(B) фиксация выполненного домашнего задания** (прогресс, снимок/итог — в терминах [data-model.md](../data-model.md)). Зафиксировать форматы ошибок и подход к версии API (путь или заголовки).

### Состав работ

- [ ] Описать ресурсы и потоки запросов для (A) и (B); сопоставить с `Conversation`, `Message`, `KnowledgeSnapshot` и связями из data-model.
- [ ] Поддержать актуальный **OpenAPI** (или эквивалент) в репозитории.
- [ ] Зафиксировать коды ошибок и структуру тел ошибок; политика версии API.

### Артефакты

- Файл спецификации OpenAPI (YAML/JSON) в `backend/` или `docs/`; краткое резюме сценариев при необходимости в существующих документах (без создания лишних файлов без согласования).

### Definition of Done (агент)

- Оба сценария прослеживаются в OpenAPI; нет противоречий data-model на уровне имён сущностей и смысла полей.
- Ошибки и версия описаны явно; заготовка под второй клиент (веб) учтена.

### Definition of Done (пользователь)

- Можно открыть OpenAPI и пройти сценарии (A) и (B) «на бумаге».
- Понятно, как клиент передаёт идентификатор пользователя/ученика и контекст сессии.

### Проверка после задачи

| Кто | Что |
|-----|-----|
| **Агент** | Валидация OpenAPI (инструментом или вручную); перекрёстная проверка с vision (сценарии MVP). |
| **Пользователь** | Просмотр OpenAPI; сопоставление с таблицей сценариев в vision. |
| **Команды** | При наличии — линт спецификации из Makefile. |
| **Результат** | Один источник правды по контракту до кодирования эндпоинтов. |

### Документы задачи

- 📋 План и 📝 Summary — `docs/tasks/impl/backend/iter-2-mvp/tasks/task-02-api-contracts/`

---

## Задача 03: Каркас backend-сервиса 📋

### Цель

Воспроизводимый каркас `backend/` с разделением слоёв по [vision.md](../vision.md), конфигурацией из `.env`, минимальными маршрутами (health/readiness).

### Состав работ

- [ ] Создать модули: API-роутеры, сервисы, `tutor/`, `llm/`; точка входа процесса API (отдельно от `main.py` бота при необходимости).
- [ ] Подключить `pydantic-settings` / аналог; расширить [.env.example](../../.env.example) под backend.
- [ ] Заложить порты и базовый URL для последующего клиента бота.

### Артефакты

- Каталог `backend/` с рабочим запуском «пустого» сервиса (хотя бы health).

### Definition of Done (агент)

- Сервис стартует без секретов в коде; конфиг читается из окружения.
- Структура каталогов соответствует vision и обновлённым conventions.

### Definition of Done (пользователь)

- По README/vision понятно, какой командой поднять API (или заглушку до задачи 06).

### Проверка после задачи

| Кто | Что |
|-----|-----|
| **Агент** | Запуск процесса API локально; запрос к health возвращает ожидаемый статус. |
| **Пользователь** | Запуск по документированной команде; проверка health в браузере или curl. |
| **Команды** | Цели в Makefile по мере появления (`make run-backend` и т.д.). |
| **Результат** | Процесс API отделён от бота; порт не конфликтует с типовым окружением. |

### Документы задачи

- 📋 План и 📝 Summary — `docs/tasks/impl/backend/iter-2-mvp/tasks/task-03-backend-scaffold/`

---

## Задача 04: Базовые API-тесты ✅

### Цель

Автоматические тесты API для сценариев, которые **уже доступны через бота**: паритет ожидаемого поведения, happy-path, представительные ошибки валидации.

### Состав работ

- [x] Настроить прогон интеграционных тестов API (async-клиент к тестовому приложению — по стеку).
- [x] Покрыть сценарии (A) и (B) минимум на уровне, достаточном для регрессии при изменении контрактов.

### Артефакты

- Тесты в репозитории; при необходимости фикстуры и тестовая конфигурация.

### Definition of Done (агент)

- Тесты зелёные в CI или локально одной командой из Makefile/README.
- Падение тестов при ломающем изменении контрактов по сценариям бота маловероятно останется незамеченным.

### Definition of Done (пользователь)

- Одна команда (например `make test`) выполняется без ручной настройки сверх описанного в README.

### Проверка после задачи

| Кто | Что |
|-----|-----|
| **Агент** | Полный прогон тестов после реализации заглушек/реализации в задаче 05. |
| **Пользователь** | `make test` (или задокументированный аналог). |
| **Команды** | Добавить/обновить цель в Makefile при введении команды тестов. |
| **Результат** | В отчёте pytest видно покрытие ключевых маршрутов. |

### Документы задачи

- 📋 План и 📝 Summary — `docs/tasks/impl/backend/iter-2-mvp/tasks/task-04-api-tests/`

---

## Задача 05: Реализация endpoint'ов и серверной логики ✅

### Цель

Реализовать контракты OpenAPI с валидацией; состояние — **в памяти** до ит.3. Централизовать вызовы **OpenRouter** в backend: LLM-клиент **stateless**, историю сообщений собирает tutor/сессия ([vision.md](../vision.md), [integrations.md](../integrations.md)).

### Состав работ

- [x] Реализовать эндпоинты по OpenAPI; согласованность тел ошибок с задачей 02.
- [x] Подключить OpenAI-совместимый клиент к OpenRouter из конфига; не хранить историю внутри LLM-клиента.
- [x] Обеспечить сценарии (A) и (B) end-to-end на уровне API.

### Артефакты

- Код `backend/` (роутеры, сервисы, tutor, llm); обновления `.env.example` для ключей и URL провайдера.

### Definition of Done (агент)

- Все критичные пути из задачи 04 покрыты реализацией; тесты зелёные.
- Секреты не попадают в логи; `LOG_LEVEL` учитывается.

### Definition of Done (пользователь)

- Можно вызвать API (curl/httpie) по сценариям (A) и (B) и получить осмысленный ответ при валидном `.env`.

### Проверка после задачи

| Кто | Что |
|-----|-----|
| **Агент** | `make test`; ручной smoke по двум сценариям; проверка логов на отсутствие утечек. |
| **Пользователь** | Повторить smoke или довериться тестам; проверить пример запросов в README/OpenAPI. |
| **Команды** | `make test`, запуск API; при необходимости `make run-backend`. |
| **Результат** | Ответы API соответствуют OpenAPI; LLM вызывается из backend, не из бота. |

### Документы задачи

- [план](impl/backend/iter-2-mvp/tasks/task-05-api-implementation/plan.md) \| [summary](impl/backend/iter-2-mvp/tasks/task-05-api-implementation/summary.md)

---

## Задача 06: Документирование backend 📋

### Цель

Один понятный сценарий: как установить зависимости, задать переменные, запустить API, открыть OpenAPI; документация синхронизирована с кодом. Актуализировать перечисленные проектные файлы (см. матрицу выше).

### Состав работ

- [ ] Описать в [README.md](../../README.md) и при необходимости в vision: запуск backend, порт, получение OpenAPI (URL или файл).
- [ ] Обновить [.env.example](../../.env.example) полным списком переменных для backend и бота.
- [ ] Привести в соответствие [docs/vision.md](../vision.md), [docs/data-model.md](../data-model.md), [docs/integrations.md](../integrations.md), [docs/plan.md](../plan.md) — что уже сделано по ит.2 (без переписывания дорожной карты целиком).

### Артефакты

- Обновлённые `README.md`, `.env.example`, релевантные файлы в `docs/`.

### Definition of Done (агент)

- Новый разработчик по README поднимает API без разгадки «по коду».
- OpenAPI доступен или путь к файлу указан; переменные окружения документированы.

### Definition of Done (пользователь)

- Следуя README, поднять backend и открыть документацию API (Swagger UI или статический файл).

### Проверка после задачи

| Кто | Что |
|-----|-----|
| **Агент** | Чистая ветка: install → copy `.env.example` → run → health + OpenAPI. |
| **Пользователь** | То же с машины разработчика. |
| **Команды** | Все задействованные цели перечислены в README и в Makefile. |
| **Результат** | Документация и факт запуска совпадают. |

### Документы задачи

- 📋 План и 📝 Summary — `docs/tasks/impl/backend/iter-2-mvp/tasks/task-06-backend-docs/`

---

## Задача 07: Рефакторинг бота под backend API ✅

### Цель

Бот — тонкий клиент: Telegram ↔ HTTP к backend; прямые вызовы `tutor`/`llm` из обработчиков убрать. Согласовать идентификацию пользователя с API.

### Состав работ

- [x] Ввести HTTP-клиент к backend (общий модуль при необходимости).
- [x] Перевести сценарии сообщений, уже работающие в боте, на API.
- [x] Обновить [README.md](../../README.md) / vision при смене команды запуска «бот + backend».

### Артефакты

- Изменения в `bot/`; при необходимости общий пакет клиента.

### Definition of Done (агент)

- Нет дублирования доменной логики между ботом и backend для сценариев ит.2.
- Тесты API (задача 04) остаются валидными; при необходимости — e2e-сценарий «бот + API» задокументирован.

### Definition of Done (пользователь)

- `make run` (или задокументированная пара команд) даёт работающий диалог в Telegram через backend.

### Проверка после задачи

| Кто | Что |
|-----|-----|
| **Агент** | Локальный прогон: backend + бот; просмотр логов на корректные запросы. |
| **Пользователь** | Ручной диалог в Telegram по сценариям (A) и (B). |
| **Команды** | Зафиксированы в Makefile и README (два процесса или compose — по принятому решению). |
| **Результат** | Поведение бота соответствует прежним сценариям при новой архитектуре. |

### Документы задачи

- [план](impl/backend/iter-2-mvp/tasks/task-07-bot-refactor-api/plan.md) \| [summary](impl/backend/iter-2-mvp/tasks/task-07-bot-refactor-api/summary.md)

---

## Задача 08: Базовое качество и инженерные практики ✅

### Цель

Закрепить минимальный стандарт качества для backend: линтер (`ruff`), осмысленное логирование, политика изменения контрактов; все новые команды отражены в Makefile и документации.

### Состав работ

- [x] Единый стиль и проверки для `backend/` (и точек интеграции с ботом по согласованию).
- [x] Структурированные логи; идентификаторы запроса/пользователя где уместно; без секретов в логах.
- [x] Проверить, что Makefile содержит цели `lint`, `format`, `test`, `run-backend` (или актуальные имена) и что они описаны в README/vision.

### Артефакты

- Настройки ruff/pytest по необходимости; обновлённый Makefile.

### Definition of Done (агент)

- `make lint` и `make test` проходят на чистом дереве.
- Нет расхождений между документацией команд и Makefile.

### Definition of Done (пользователь)

- Можно воспроизвести проверки качества по README одной-двумя командами.

### Проверка после задачи

| Кто | Что |
|-----|-----|
| **Агент** | Прогон lint + test в CI или локально; сверка списка целей Makefile с README. |
| **Пользователь** | Запуск `make lint` / `make test`. |
| **Команды** | `make lint`, `make format`, `make test`, `make check` (+ перечислить фактические). |
| **Результат** | Качество воспроизводимо; контракты меняются осознанно (OpenAPI + тесты). |

### Документы задачи

- [план](impl/backend/iter-2-mvp/tasks/task-08-quality/plan.md) \| [summary](impl/backend/iter-2-mvp/tasks/task-08-quality/summary.md)

---

### Пометка: завершение итерации 2

| | |
|--|--|
| **Агент проверяет** | Все задачи 01–08: тесты зелёные, линт чистый, OpenAPI соответствует коду, бот ходит только в API для доменных сценариев. |
| **Пользователь проверяет** | README: установка, `.env.example`, две команды запуска (API + бот); ручной прогон сценариев в Telegram. |
| **Команды** | `make install`, `make test`, `make lint`, запуск API и бота по README. |
| **Где смотреть результат** | OpenAPI/Swagger, логи сервиса, ответы в Telegram; отсутствие прямых вызовов LLM из `bot/`. |

---

## Итерация 3: персистентность (следующая волна)

### Задача 09: Персистентность и модель данных 📋

**Цель:** пользователи, диалоги, агрегаты прогресса между перезапусками; миграции переносимы ([ADR-001](../adr/adr-001-database.md), [data-model.md](../data-model.md)).

**Состав работ:** слой доступа к данным; миграции; сопоставление сущностей с data-model; уточнение data-model при необходимости.

**DoD (агент):** восстановление после рестарта; тесты репозиториев или контрактные с миграциями; нет критичной потери данных при штатном перезапуске.

**DoD (пользователь):** сценарии ит.2 воспроизводимы после рестарта сервиса с БД.

**Проверка:** миграции на чистой БД; бэкап/восстановление — по плану ит.6/infra tasklist.

**Документы:** `docs/tasks/impl/backend/iter-3-persistence/tasks/task-09-database/` — план \| summary (после создания)

| Статус | 📋 Planned |
|--------|------------|

---

## Итерация 5: расширение платформы

### Задача 10: Расширение платформы 📋

**Цель:** инкременты из [idea.md](../idea.md) (преподаватель, материалы, потоки/группы) без ломки контрактов для существующих клиентов; при значимых решениях — [docs/adr/](../adr/README.md).

**Состав работ:** декомпозиция по приоритету; согласование с [tasklist-web.md](tasklist-web.md) при затрагивании UI.

**DoD (агент):** выбранный инкремент работает end-to-end через API; OpenAPI обновлён; обратная совместимость или явная версия.

**DoD (пользователь):** проверка сценария инкремента в согласованном интерфейсе.

**Документы:** `docs/tasks/impl/backend/iter-5-platform/tasks/...` — план \| summary (после создания)

| Статус | 📋 Planned |
|--------|------------|

---

## Качество и инженерные практики (резюме)

- **Тесты:** минимум happy-path и ошибки валидации для API; после БД — репозитории или контрактные тесты с миграциями.
- **Стиль:** `ruff` ([vision.md](../vision.md)); один нетривиальный класс — один модуль.
- **Наблюдаемость:** уровни логов для эксплуатации; без утечки секретов.
- **Контракты:** обратная совместимость или явная версия API; крупные ломающие изменения — ADR и обновление OpenAPI до выката клиентов.
